// Comentando todo o conteúdo
/*
import { User } from '@clerk/clerk-sdk-node'; // Importação Node.js, não usar no Expo

export interface AuthUser {
  id: string;
  email: string | null;
  name: string | null;
  firstName: string | null;
  lastName: string | null;
  imageUrl: string | null;
  // Adicione outros campos que você precisa do usuário autenticado
}

// Interface base para serviços de autenticação
export interface AuthService {
  verifyTokenAndGetUser(token: string): Promise<AuthUser | null>;
  // Outros métodos de autenticação podem ser definidos aqui (login, register, etc.)
  // Se você estiver usando APENAS Clerk no frontend, muitos métodos podem não ser necessários aqui
}
*/
export {}; // Export vazio
